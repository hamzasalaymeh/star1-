# 📋 الأوامر الأساسية

## التثبيت

```bash
npm install
```

---

## ▶️ تشغيل الأداة

### 1️⃣ الطريقة الأفضل - واجهة تفاعلية

```bash
node exporter-cli.js
```

**الخطوات:**
1. أدخل اسم المستخدم
2. أدخل كلمة المرور
3. أدخل أرقام القرارات (مثال: 21، 22، 23)

**مثال:**
```
👤 اسم المستخدم: admin
🔑 كلمة المرور: ••••••••
📋 أدخل أرقام قرارات التسجيح:
رقم قرار التسجيل #1: 21
رقم قرار التسجيل #2: 22
رقم قرار التسجيل #3: 23
رقم قرار التسجيل #4: (اضغط Enter)

✅ سيتم تصدير 3 قرار
هل تريد المتابعة؟ (نعم/لا): نعم
```

---

### 2️⃣ من سطر الأوامر (لا تفاعل)

```bash
node table-pdf-exporter-advanced.js admin password123
```

**ملاحظة:** الأداة ستختار القيم الافتراضية إذا لم تدخل أرقام قرارات.

---

### 3️⃣ استكشاف الأخطاء (debug mode)

```bash
node debug-exporter.js admin password123 21
```

**يفعل:**
- يعرض المتصفح على الشاشة
- يسجل كل الخطوات بالتفصيل
- يحفظ لقطات شاشة في مجلد `debug-exports/`

---

## 🧪 تشغيل الأمثلة

### مثال 1: الاستخدام الأساسي

```bash
node example-usage.js 1
```

---

### مثال 2: إعدادات مخصصة

```bash
node example-usage.js 2
```

---

### مثال 3: خطوات يدوية

```bash
node example-usage.js 3
```

---

### مثال 4: معالجة دفعة كبيرة

```bash
node example-usage.js 4
```

---

## 📂 فحص المخرجات

### عرض الملفات المُصدَّرة

```bash
ls -lh exports/
```

**النتيجة:**
```
قرار_21_1691234567890.pdf  (2.5M)
قرار_21_1691234567890.png  (8.7M)
قرار_22_1691234567899.pdf  (2.6M)
قرار_22_1691234567899.png  (8.9M)
...
```

### فحص ملفات التصحيح

```bash
ls -lh debug-exports/
```

---

## 🛠️ تعديل الإعدادات

### تشغيل مع عرض المتصفح

عدّل هذا السطر في `exporter-cli.js`:

```javascript
const exporter = new AdvancedTablePDFExporter(username, password, {
  headless: false  // غيّرها من true إلى false
});
```

---

### زيادة المهلة الزمنية

```javascript
const exporter = new AdvancedTablePDFExporter(username, password, {
  navigationTimeout: 60000  // 60 ثانية بدلاً من 30
});
```

---

### تغيير مجلد المخرجات

```javascript
const exporter = new AdvancedTablePDFExporter(username, password, {
  outputDir: './my-custom-exports'
});
```

---

## 🔄 العملية الكاملة (Step-by-Step)

```bash
# 1. اذهب للمجلد
cd /path/to/star1-

# 2. ثبّت المتطلبات
npm install

# 3. شغّل الأداة
node exporter-cli.js

# 4. اتّبع الخطوات المطلوبة

# 5. انتظر حتى ينتهي

# 6. افتح الملفات
open exports/  # macOS
# أو
explorer exports\  # Windows
# أو
nautilus exports/  # Linux
```

---

## ✅ التحقق من النجاح

بعد تشغيل الأداة، يجب أن تراي:

```
✅ تم تسجيل الدخول
📋 جارٍ إدخال رقم قرار التسجيل: 21
📂 جارٍ فتح الاستمارة الأولى...
✅ تم فتح الاستمارة
📊 جارٍ الانتقال إلى معايير التصنيف...
✅ تم الانتقال إلى معايير التصنيف
🔍 جارٍ البحث عن الجدول المحدد بمربع أحمر...
✅ تم العثور على الجدول: 1200x2000px
📄 جارٍ تحويل الجدول إلى PDF
✅ تم التصدير: ./exports/قرار_21_1691234567890.pdf
📸 جارٍ حفظ لقطة شاشة الجدول...
✅ تم حفظ لقطة الشاشة: ./exports/قرار_21_1691234567890.png

📊 النتيجة النهائية: 3/3
🎉 تم التصدير بنجاح!
```

---

## ❌ حل المشاكل

### مشكلة: "لم يتم العثور على الجدول"

```bash
# الحل: استخدم debug mode
node debug-exporter.js admin password 21

# وتحقق من لقطات الشاشة
ls debug-exports/
```

---

### مشكلة: "فشل تسجيل الدخول"

```bash
# تأكد من بيانات الدخول:
node debug-exporter.js admin password 21

# قد تحتاج المنصة توثيق إضافي
```

---

### مشكلة: "انتهاء المهلة الزمنية"

```javascript
// في exporter-cli.js أو أي ملف:
const exporter = new AdvancedTablePDFExporter(username, password, {
  navigationTimeout: 90000  // زيادة إلى 90 ثانية
});
```

---

## 📊 معلومات إضافية

### عدد القرارات
- الحد الأدنى: 1 قرار
- الحد الأقصى: بدون حد (لكن قد يأخذ وقتاً)

### حجم الملفات
- PDF الواحد: ~2-3 MB
- PNG الواحدة: ~8-10 MB

### الوقت المتوقع
- قرار واحد: ~20-30 ثانية
- 5 قرارات: ~2-2.5 دقيقة
- 10 قرارات: ~4-5 دقائق

---

## 🎯 نصائح

1. **ابدأ بقرار واحد** عشان تتأكد من اشتغال كل شيء
2. **استخدم debug mode** إذا حصلت مشكلة
3. **الأداة تعمل في الخلفية** - لا تعطل عملك
4. **تابع المسار** بفتح `exports/` بعد الانتهاء

---

**كل سؤال؟ اقرأ `QUICK_START.md` أو `README_AR.md` 📚**
