# 🚀 دليل البدء السريع

## التثبيت

```bash
npm install
```

## 1️⃣ الطريقة الأسهل - واجهة تفاعلية

```bash
node exporter-cli.js
```

ثم أدخل:
1. **اسم المستخدم** - مثال: `admin`
2. **كلمة المرور** - مثال: `password123`
3. **أرقام القرارات** - واحد في كل سطر (مثال: `21` ثم `22` ثم `23`)

```
👤 اسم المستخدم: admin
🔑 كلمة المرور: ••••••••
📋 أدخل أرقام قرارات التسجيل:
رقم قرار التسجيل #1: 21
رقم قرار التسجيل #2: 22
رقم قرار التسجيل #3: 23
رقم قرار التسجيل #4: (اضغط Enter للانتهاء)
```

---

## 2️⃣ من سطر الأوامر

```bash
node table-pdf-exporter-advanced.js username password
```

---

## 3️⃣ استكشاف الأخطاء

اذا واجهت مشاكل، استخدم أداة التصحيح:

```bash
node debug-exporter.js admin password123 21
```

هذا سيفتح المتصفح وسيريك كل خطوة + يحفظ لقطات شاشة في `debug-exports/`

---

## 📁 ملفات المخرجات

كل شيء يُحفظ في مجلد `exports/`:

```
exports/
├── قرار_21_1691234567890.pdf
├── قرار_21_1691234567890.png
├── قرار_22_1691234567899.pdf
├── قرار_22_1691234567899.png
└── ...
```

---

## 🔄 خطوات العملية

الأداة تقوم بهذه الخطوات تلقائياً:

1. ✅ تسجيل دخول أوتوماتيكي
2. ✅ إدخال رقم قرار التسجيل (مثل: 21)
3. ✅ فتح الاستمارة الأولى من قائمة الاستمارات
4. ✅ الانتقال إلى قسم "معايير التصنيف"
5. ✅ تحميل كل البيانات (3000+ سجل)
6. ✅ استخراج الجدول الأحمر
7. ✅ تصدير PDF + لقطة شاشة

---

## ⚙️ إعدادات متقدمة

### تشغيل مع عرض المتصفح (للتصحيح):

عدّل `exporter-cli.js`:

```javascript
const exporter = new AdvancedTablePDFExporter(username, password, {
  headless: false,  // اترك true للخلفية، false لعرض المتصفح
  outputDir: './my-exports'
});
```

### زيادة وقت الانتظار:

```javascript
const exporter = new AdvancedTablePDFExporter(username, password, {
  navigationTimeout: 60000  // 60 ثانية بدلاً من 30
});
```

---

## 💡 نصائح

- الأداة تعمل **في الخلفية** لا تعطل عملك
- كل قرار تقريباً **3000 استمارة**
- تصدير PDF + لقطة شاشة **تلقائياً**
- إذا فشل شيء، جرب **debug mode** للرؤية الفعلية

---

## 🆘 مشاكل شائعة

### المشكلة: "لم يتم العثور على الجدول"
- ✓ استخدم `debug-exporter` عشان تشوف ما يحصل
- ✓ تأكد من رقم القرار صحيح

### المشكلة: "فشل تسجيل الدخول"
- ✓ تحقق من اسم المستخدم وكلمة المرور
- ✓ قد تحتاج المنصة توثيق إضافي

### المشكلة: "Connection timeout"
- ✓ اتصالك بالإنترنت بطيء
- ✓ زيادة `navigationTimeout` في الإعدادات

---

## 📚 أمثلة إضافية

جرب الأمثلة المدرجة:

```bash
node example-usage.js 1  # الاستخدام الأساسي
node example-usage.js 2  # إعدادات مخصصة
node example-usage.js 3  # خطوات يدوية
node example-usage.js 4  # معالجة دفعة كبيرة
```

---

**كل شيء تحت السيطرة - استرخ واترك الأداة تعمل! 🎉**
