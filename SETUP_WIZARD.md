# 🧙 معالج الإعداد الأولي

دعني أستكشف الواجهة الفعلية وأعرف بالضبط وين كل حاجة.

## الخطوة 1️⃣: تشغيل أداة الفحص

```bash
node inspector-tool.js admin password123
```

**هذا سيفعل:**
1. ✅ يفتح المتصفح
2. ✅ يعرض صفحة تسجيل الدخول
3. ✅ يفحص جميع الحقول والأزرار
4. ✅ ينتظرك لتسجيل الدخول يدوياً
5. ✅ بعدها يفحص صفحة النتائج

---

## الخطوة 2️⃣: أجب على الأسئلة

بعد التشغيل، ستسأل الأداة:

### ❓ السؤال 1: أين حقل رقم القرار؟

**ابحث عن:**
- حقل إدخال (input) بحد أحمر
- أو حقل فيه placeholder يقول "رقم" أو "قرار"

**الإجابة:**
```
في الجزء العلوي يسار الصفحة، حقل بحد أحمر بسيط
```

---

### ❓ السؤال 2: أين زر الإدخال/البحث؟

**ابحث عن:**
- زر أخضر أو أزرق بعد حقل البحث
- أو زر فيه كلمة "بحث" أو "إدخال"

**الإجابة:**
```
بجانب الحقل الأحمر، زر أخضر يقول "تطبيق" أو مشابه
```

---

### ❓ السؤال 3: ما اسم التبويب للتصنيف؟

**ابحث عن:**
- قائمة تبويبات (tabs) في الصفحة
- كلمة تشبه "معايير" أو "تصنيف" أو "classification"

**الإجابة:**
```
في شريط التنقل العلوي: "معايير التصنيف" أو "التصنيف"
```

---

### ❓ السؤال 4: هل الجدول منفصل؟

**ابحث عن:**
- الجدول الأحمر في نفس الصفحة؟
- أم بعد ضغط على رابط أو تبويب؟

**الإجابة:**
```
في نفس الصفحة بعد إدخال رقم القرار
أو في صفحة منفصلة بعد الضغط على التبويب
```

---

## الخطوة 3️⃣: أرسل لي المعلومات

بعد فحص الأداة، أرسل لي:

```
1. اسم حقل رقم القرار (ID أو name)
   المثال: id="decisionNumber" أو name="decision"

2. اسم زر البحث
   المثال: id="searchBtn" أو class="btn-primary"

3. اسم التبويب التصنيف
   المثال: "معايير التصنيف" أو "التصنيف"

4. أين الجدول؟
   في نفس الصفحة ✓ أم منفصل ✗
```

---

## النتيجة النهائية

بعد الإجابات، بحدّث الأوتوميشن بالمعلومات الدقيقة:

```javascript
// في exporter-cli.js أو أي مكان:

const SELECTORS = {
  // حقول الإدخال
  decisionInput: '#decisionNumber',      // ← معلومتك
  
  // الأزرار
  searchButton: '#searchBtn',             // ← معلومتك
  submitButton: 'button[type="submit"]',
  
  // التنقل
  classificationTab: 'div:contains("معايير التصنيف")',  // ← معلومتك
  
  // الجداول
  redTable: 'div[style*="border: 2px solid red"]',
  mainTable: 'table.main-table'
};
```

---

## مثال بسيط

**قبل (عام وغير دقيق):**
```javascript
const input = await this.page.$('input[type="text"]');
```

**بعد (دقيق):**
```javascript
const input = await this.page.$('#decisionNumber');
```

---

## 🎯 الخطوات

1. `node inspector-tool.js admin password`
2. شوف output الأداة
3. أجب على الأسئلة الـ 4
4. أرسل لي المعلومات
5. بحدّث الكود

**في 5 دقائق بنكون دقيقين 100% ✓**

---

اي سؤال؟ 🤔
